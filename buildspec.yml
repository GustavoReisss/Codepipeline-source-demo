version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Starting Install Phase"
      - cd app
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - "app/dist/*"
  discart-paths: yes
